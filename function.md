# function

**Function pipe for chain methods:** Function can recibe arguments and pass in callback arguments function.

```javascript
sb.function("Merlin", name => {
    return `Hi ${name}`
});
```

```text
-> "Hi Merlin"
```

